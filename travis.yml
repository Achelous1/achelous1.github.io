language: ruby
rvm:
- 2.2.0
sudo: false
script: bundle exec jekyll build && bundle exec rake deploy 
addons:
  apt:
    packages:
    - graphviz 
# env:
#   global:
#     secure: <yourToken>